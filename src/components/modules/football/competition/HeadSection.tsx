export const HeadSection = ({
  tournamentId,
  seasonId,
  uniqueTournament,
}: {
  tournamentId: string;
  seasonId: string;
  uniqueTournament: boolean;
}) => {
  // useSeasonStandingsDataHead(tournamentId, seasonId, 'home', uniqueTournament);
  // useSeasonStandingsDataHead(tournamentId, seasonId, 'away', uniqueTournament);

  return <></>;
};
